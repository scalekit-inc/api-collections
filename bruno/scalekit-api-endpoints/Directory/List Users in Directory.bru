meta {
  name: List Users in Directory
  type: http
  seq: 3
}

get {
  url: {{env_url}}/api/v1/organizations/{{organization_id}}/directories/{{directory_id}}/users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
