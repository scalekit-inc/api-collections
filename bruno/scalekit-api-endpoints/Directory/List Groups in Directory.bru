meta {
  name: List Groups in Directory
  type: http
  seq: 4
}

get {
  url: {{env_url}}/api/v1/organizations/{{organization_id}}/directories/{{directory_id}}/groups
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
