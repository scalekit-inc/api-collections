meta {
  name: Get Directory
  type: http
  seq: 2
}

get {
  url: {{env_url}}/api/v1/organizations/{{organization_id}}/directories/{{directory_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
